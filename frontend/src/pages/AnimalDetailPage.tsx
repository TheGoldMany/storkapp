import { Container, Typography } from '@mui/material'

const AnimalDetailPage = () => {
  return (
    <Container maxWidth="lg">
      <Typography variant="h4">Állat részletei</Typography>
    </Container>
  )
}

export default AnimalDetailPage
